services:
    app:
        image: ttungbmt/ots-maps-frontend:${APP_VERSION:-1.0.0}
        build:
            context: .
            dockerfile: ./Dockerfile
            target: deploy
            args:
                - ENV_FILE=.env.production
        ports:
            - '${APP_HTTP_PORT:-3000}:3000'
