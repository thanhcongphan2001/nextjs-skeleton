services:
    app:
        image: repo-registry.laragis.vn/gtelmaps/embed:${APP_VERSION}
        build:
            context: .
            dockerfile: ./Dockerfile
            target: deploy
            args:
                - ENV_FILE=.env.production
        ports:
            - '${APP_HTTP_PORT:-3000}:3000'
